# Durable Memories - 2026-04-05

## Deployment Progress: budgetapp
- **Web Frontend (Vercel):** Successfully deployed at `app.ton-domaine.com` (using `apps/web`).
- **Configuration Hybride:** 
    - `Vercel` pour le front-end Next.js.
    - `Hetzner + Coolify` (en cours/prévu) pour l'API Bun/Elysia et PostgreSQL.
- **Vercel Build Fixes:**
    - Fixed Prisma Client generation during build by adding `bun --filter database generate` to `apps/web` build script.
    - Added `DATABASE_URL`, `API_URL`, `NEXT_PUBLIC_API_URL`, and `AUTH_SECRET` to Vercel and `turbo.json`.
    - Resolved `missing-suspense-with-csr-bailout` error by wrapping `/login` and `/register` pages in `<Suspense>`.
    - Renamed `proxy.ts` to `middleware.ts` in `apps/web` to comply with Next.js standards.
- **Local Database:**
    - Local DB is a Docker container (Postgres 15).
    - `DATABASE_URL` for local dev: `postgresql://postgres:mysecretpassword@localhost:5432/budget_db`.
    - Fixed `prisma.config.ts` to use `localhost` instead of a static Docker bridge IP.

## UI/UX Feedback
- **Responsive Design:** The current layout (grid-based) is broken on small mobile devices (e.g., Samsung Galaxy). Overlapping elements detected on `/login`. Mobile optimization (Media Queries) is the next priority for the frontend.
