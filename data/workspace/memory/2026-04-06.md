# Session Memory - 2026-04-06

## Project: BudgetApp (Vercel + Prisma + Coolify/PostgreSQL)

### Infrastructure & Config
- **Production DB:** PostgreSQL hosted via Coolify on Hetzner VPS (`46.225.158.215`).
- **Connection URL:** `postgres://postgres:[PASSWORD]@46.225.158.215:5432/postgres`. 
- **Setup:** The PostgreSQL service in Coolify was configured to be "Publicly Available" on port `5432` to allow Vercel and local migrations to connect.
- **Prisma Configuration:** `packages/database/prisma.config.ts` was adjusted to properly load environment variables from `.env` for `db push` operations.

### Actions Taken
- **Database Sync:** Successfully ran `bunx prisma db push` to synchronize the production database schema.
- **Authentication:** Login functionality verified (500 error resolved by fixing the database connection).

### Current Status / Issues
- **UI/Logic Mismatch:** The user reports that recent code changes (balance calculation logic) aren't reflected in the production build.
- **API Errors:** Encountering `502 Bad Gateway` and `Unexpected token '<'` (HTML returned instead of JSON) on routes like `/api/transactions` and `/api/insights/summary`.
- **Next Step:** Investigating whether the issues are caused by Cloudflare proxying, Vercel timeouts, or a stale deployment branch.
