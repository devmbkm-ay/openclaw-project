# Session Memory - 2026-04-08

## BudgetApp Project Debugging
- **Issue:** API connection failure between Vercel (Frontend) and Hetzner/Coolify (Backend).
- **Diagnosis:** 
  - Mixed Content block: Vercel (HTTPS) cannot request a backend on raw HTTP/IP.
  - Deployment tool: Next.js 16 on Vercel defaults to Turbopack, causing build errors with existing Webpack config.
- **Actions taken:**
  - Configured DNS A record `api.budgetapp.ricardomboukou.online` -> `46.225.158.215` (Vercel DNS).
  - Configured HTTPS FQDN in Coolify for `budgetapp-api`.
  - Updated `apps/web/next.config.js` to support Turbopack via `experimental.turbo` alias rules.
- **Status:** Pending SSL propagation/generation in Coolify (Traefik) and subsequent Vercel redeployment.
# Durable Memories - 2026-04-08

## Infrastructure & Connectivity
- **API URL Change:** Migrated BudgetApp API from `api.budgetapp.ricardomboukou.online` to `api-budgetapp.ricardomboukou.online` to resolve Cloudflare SSL wildcard limitations (multilevel subdomains not covered on free plan).
- **Hetzner IP:** Primary server IP is `46.225.158.215`.
- **Environment Consistency:** Updated `NEXT_PUBLIC_API_URL` across frontend and Docker config to point to the new Cloudflare-proxied HTTPS address.
- **Database Connection:** Standardized internal DB connection string to use `db:5432` service name instead of static IPs.

## BudgetApp Development
- **Branch:** Working on `dev-backservice`.
- **UX Improvement:** Made transaction labels optional for "Income" types. If empty, it defaults to the category name (e.g., "Salaire").
- **Design:** Implemented mobile-first responsive layout for the login/auth shell, switching from side-by-side grid to vertical stack.
- **AI Roadmap:** Brainstormed features: trend analysis, receipt OCR, and conversational financial coaching.
