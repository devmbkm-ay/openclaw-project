# Session Log: 2026-03-24

## Project: Enidpath (/projects/enidpath)
- **Domain Config:** Configured `enidpath.com` on cPanel for Vercel.
  - A Record: `76.76.21.21`
  - CNAME (www): `cname.vercel-dns.com.`
- **CMS Integration:** Installed Payload CMS 3.0 (Open Source).
  - Stack: Next.js 16 (Canary), React 19.2, MongoDB (Docker), Sharp, Lexical.
  - Configuration: Created `payload.config.ts` with Users, Media, and Pages collections.
  - Architecture: Refactored `src/app` into `(site)` and `(payload)` route groups to isolate Payload's root layout and styles from the main site's ThemeProvider/Providers, fixing hydration and script tag errors.
- **Database:** Added MongoDB service to `docker-compose.yml` and updated `.env` with `MONGODB_URI` and `PAYLOAD_SECRET`.
- **Troubleshooting:**
  - Fixed "ERESOLVE" dependency conflicts using `--legacy-peer-deps`.
  - Fixed "Module not found: graphql" by installing `graphql`.
  - Fixed "Functions cannot be passed directly to Client Components" by making Payload's `RootLayout` synchronous and passing the config object directly.
  - Fixed unstyled admin UI by importing `@payloadcms/next/css`.
  - Fixed nested `<html>`/`<body>` tags by moving root tags into group layouts.

