# 2026-03-28 - Durable Memories

## Portfolio Improvements (RicardoMBoukou)

### Theme & Design
- **Theme Toggle:** Implemented a light/dark mode switch in the Navbar.
- **Variables CSS:** Refactored `globals.css` to use theme variables for background, foreground, primary, accent, and muted colors.
- **Electric Aesthetic:** Preserved the "Electric Blue/Cyan" look in dark mode while providing a clean Light mode alternative.

### Internationalization (i18n)
- **Bilingual Schema:** Migrated Prisma `Project` model from single `title/description` to `titleEn`, `titleFr`, `descriptionEn`, and `descriptionFr`.
- **Logic Sync:** Updated `ProjectCard`, `ProjectDetailClient`, and `ProjectListClient` to conditionally render content based on the active locale (`lang`).
- **Translation Quality:** Rewrote French dictionary entries to a more senior, strategic, and professional tone ("Architecte Logiciel", "Systèmes résilients", "Augmentation humaine").
- **AI-Powered Admin:** Updated the `analyze-repo` API to automatically generate project summaries in both English and French using Gemini.

### Fixes & Stability
- **Language "Duo" Fix:** Resolved the issue where both English and French descriptions were appearing simultaneously on the project detail page.
- **Next.js Cache Clear:** Resolved a Turbopack/Next.js "Module factory not available" error by clearing the `.next` directory and regenerating the Prisma client.
- **Seeding:** Updated `prisma/seed.ts` and `prisma/data/projects.ts` to support the new bilingual structure.

### Admin Dashboard
- **Bilingual Management:** Updated the Admin Dashboard and `ProjectModal` to allow editing/creating projects with separate EN/FR fields.
- **Theme Support:** Ensured the Admin UI (Modals, Dashboards) is fully themed and compatible with both Light and Dark modes.
