# Durable Memories - 2026-04-10

## Project: BudgetApp (Portfolio)

### Features & Evolutions
- **Investment Hub Implementation**: Created an investment simulator that converts monthly savings (surplus) into Bitcoin (Satoshis) based on real-time market data.
- **BTC Price Integration**: Implemented a price engine using CoinGecko API to fetch current BTC/EUR rates.
- **Resilient AI Analysis**: 
    - Implemented an AI fallback mechanism for the transaction analysis service. 
    - When OpenAI quotas (429) are reached, the system now automatically switches to Google Gemini (gemini-1.5-flash-latest).
    - Optimized the AI service to work via direct `fetch` calls to Google Generative Language API, avoiding heavy SDK dependencies and lockfile conflicts on Vercel.

### Technical Troubleshooting (Vercel & AI)
- **404 Errors on Gemini**: Resolved by switching from `gemini-pro` to `gemini-1.5-flash-latest` and ensuring compatibility with the `v1beta` endpoint.
- **Vercel Build Issues**: Fixed a "frozen lockfile" error by removing unnecessary SDK imports in the frontend and using direct HTTP requests for AI services.
- **Runtime Errors**: Added null-checks and defensive programming in the `InvestmentOpportunityCard` to prevent crashes when data is loading or missing.

### Infrastructure & Environment
- Added `GEMINI_API_KEY` requirement for production (Vercel) to support the AI fallback system.
- Confirmed the use of `v1beta` as the active endpoint for Gemini Flash integration via fetch.
## 2026-04-10 14:13 UTC - BudgetApp Gemini API Fixes

- **Context**: Ricardo updated the Gemini API key for `budgetapp` but encountered multiple errors (404 and 500) during the transition.
- **Problem 1 (Model Availability)**: The initial model `gemini-2.0-flash` was unavailable for the new API key/region.
- **Problem 2 (API Versioning)**: Mismatch between `v1` and `v1beta` endpoints led to 404 errors when attempting to use `gemini-1.5-flash` or `gemini-1.5-flash-latest`.
- **Solution Implemented**:
    - Created a multi-model fallback strategy in `apps/web/app/api/ai/analyze/route.ts` (Commit `c21bdcc`).
    - The code now attempts `gemini-1.5-flash` (v1), then `gemini-1.5-pro` (v1), then `gemini-pro` (v1) before failing.
    - Updated logs to include detailed `[DEBUG]` and `[CRITICAL]` prefixes for easier Vercel troubleshooting.
- **Infrastructure**:
    - Confirmed `GEMINI_API_KEY` is configured in Vercel environment variables.
    - Advised keeping "Application Restrictions" to "None" in GCP Console for server-side calls from Vercel while keeping the "API Restriction" limited to "Gemini API".
- **Status**: OpenAI quota is currently exhausted (`insufficient_quota`), making the Gemini fallback critical for production features.
